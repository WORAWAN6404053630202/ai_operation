#/Users/w.worawan/Downloads/ai-operation-microservice3_v2ori/docker-compose.staging.yaml
version: '3.9'

services:
  serve:
  
    container_name: ${CONTAINER_NAME}
    image: ${DOCKER_REPO_USER}/${IMAGE_NAME}:${IMAGE_VERSION}
    platform: linux/amd64
    build:
      context: .
      target: staging
      dockerfile: ./Dockerfile
    ports:
      - "${PORT}:3000"
    volumes:
      - ./code:/app
    env_file:
      - ./env.properties 
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 40s
    restart: unless-stopped

networks:
  import-fastapi-network:
